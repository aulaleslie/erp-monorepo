import { CheckInView } from "@/components/attendance/CheckInView";

export const metadata = {
    title: "Member Check-in",
    description: "Daily member check-in and attendance tracking",
};

export default function CheckInPage() {
    return <CheckInView />;
}
