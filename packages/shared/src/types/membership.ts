export enum MembershipStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  CANCELLED = 'CANCELLED',
}

export enum MembershipHistoryAction {
  CREATED = 'CREATED',
  EXTENDED = 'EXTENDED',
  CANCELLED = 'CANCELLED',
  EXPIRED = 'EXPIRED',
  FLAGGED = 'FLAGGED',
  CLEARED = 'CLEARED',
}
